import { Location, Uri, ExtensionContext, Position, CancellationToken, TextDocument, languages, Range } from 'vscode';


class Parser {
  static parseYml(rawYml: string): YmlItem[] {
    return [];
  }
}